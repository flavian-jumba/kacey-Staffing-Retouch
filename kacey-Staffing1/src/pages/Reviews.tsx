// This page has been removed as per request to remove all reviews from the website.
import React from 'react';

const Reviews: React.FC = () => (
  <div style={{ padding: '2rem', textAlign: 'center' }}>
    <h1>Reviews Page Removed</h1>
    <p>The reviews section is no longer available.</p>
  </div>
);

export default Reviews;
